def reverse_string(s):
    left = 0
    right = len(s) - 1
    while left < right:
        s[left], s[right] = s[right], s[left]
        left += 1
        right -= 1
    return s
s = list(input("Enter the letters of the word here: ").lower().split())
print(reverse_string(s))



