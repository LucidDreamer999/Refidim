num1 = input("Enter num1 here: ")
num2 = input("Enter num2 here: ")
n = 0
one = num1[::-1]
two = num2[::-1]
add = []
for i in num1:
    for j in num2:
        mul = (ord(one[n]) - ord('0')) * (ord(two[n]) - ord('0'))
        add.append(mul)
print(add)

